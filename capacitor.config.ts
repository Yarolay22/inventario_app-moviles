import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'inventario_app-moviles',
  webDir: 'dist'
};

export default config;
